import 'styled-components';
import { DefaultTheme } from 'styled-components';

import { themeColors, themeFonts, themeSpaces } from './values';

export const theme: DefaultTheme = {
  colors: themeColors,
  fonts: themeFonts,
  spaces: themeSpaces,
};
