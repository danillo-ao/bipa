import 'styled-components';
import { DefaultTheme } from 'styled-components';

import { themeColors, themeFonts, themeRadii, themeSpaces, themeTransitions } from './values';

export const theme: DefaultTheme = {
  colors: themeColors,
  fonts: themeFonts,
  spaces: themeSpaces,
  radii: themeRadii,
  transitions: themeTransitions,
};
